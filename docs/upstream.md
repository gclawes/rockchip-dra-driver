# Upstream tracking

This driver is based on the structure of
[`kubernetes-sigs/dra-example-driver`](https://github.com/kubernetes-sigs/dra-example-driver),
not a git fork. Check that repository (and `k8s.io/dynamic-resource-allocation`)
on each Kubernetes minor and each example-driver release.

See [AGENTS.md](../AGENTS.md) for what to port and what to skip.

## Skipped upstream changes

None yet. Record example-driver commits or KEPs we evaluated and chose not to
take here.
